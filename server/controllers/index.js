const exchanges = require('./exchanges');
const tickers = require('./tickers');
const exchangePairs = require('./exchangePairs');
const wallets = require('./wallets');

module.exports = {
    exchanges,
    tickers,
    exchangePairs,
    wallets,
};